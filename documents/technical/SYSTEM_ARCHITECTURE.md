# 🎯 Mini-Agent System Flow - Visual Guide

## 🚀 **Launch Flow**

```
┌─────────────────────────────────────────────────────────┐
│  USER COMMAND                                           │
│  python launch_mini_agent.py --workspace .             │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  launch_mini_agent.py                                   │
│  1. Set Python paths                                   │
│  2. Load .env variables                                │
│  3. Import mini_agent.cli.main()                       │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  mini_agent.cli.main()                                 │
│  1. Load configuration                                 │
│  2. Initialize LLM client                              │
│  3. Load all tools                                     │
│  4. Start interactive session                          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  CONFIGURATION LOADING                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │ .env        │  │ config.yaml │  │ mcp.json    │      │
│  │ ZAI_API_KEY │  │ LLM settings│  │ MCP servers │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  TOOL REGISTRATION                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐      │
│  │ File Tools  │  │ Z.AI Tools  │  │ MCP Tools   │      │
│  │ (3 tools)   │  │ (2 tools)   │  │ (var tools) │      │
│  └─────────────┘  └─────────────┘  └─────────────┘      │
│  ┌─────────────┐  ┌─────────────┐                       │
│  │ Bash Tools  │  │ Skills      │                       │
│  │ (3 tools)   │  │ (10+ tools) │                       │
│  └─────────────┘  └─────────────┘                       │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│  READY TO USE                                           │
│  39+ tools available in interactive session            │
└─────────────────────────────────────────────────────────┘
```

## 📁 **File Structure**

```
C:\Users\Jazeel-Home\Mini-Agent\
│
├── 🚀 launch_mini_agent.py          # ⭐ MAIN LAUNCHER
├── 📝 .env                          # Environment variables
├── 📋 pyproject.toml                # Dependencies
│
├── 📂 mini_agent\                   # Core package
│   ├── 🔧 config.py                 # Configuration loader
│   ├── 💻 cli.py                    # Main CLI interface
│   ├── 🤖 agent.py                  # Agent logic
│   │
│   └── 📂 config\                   # 📋 CONFIG DIRECTORY
│       ├── ⚙️ config.yaml           # Main configuration
│       ├── 🔗 mcp.json              # MCP server config
│       ├── 💭 system_prompt.md      # System prompt
│       └── 📖 config-example.yaml   # Template
│
├── 📂 skills\                       # Built-in skills
│   ├── docx\                        # Document skills
│   ├── pdf\                         # PDF skills
│   ├── pptx\                        # PowerPoint skills
│   ├── xlsx\                        # Excel skills
│   └── [10+ other skills]
│
└── 📂 documents\                    # Documentation
    ├── 📖 CONFIGURATION_GUIDE.md    # This guide
    ├── 📖 PROJECT_STATUS.md         # Current status
    └── 📖 WORKING_SETUP.md          # Setup instructions
```

## ⚙️ **Configuration Flow**

```
┌─────────────┐
│ .env        │ ← ZAI_API_KEY, other variables
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ config.yaml │ ← LLM settings, tool toggles
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   mcp.json  │ ← MCP server configurations
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  config.py  │ ← Unified configuration loader
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   cli.py    │ ← Uses configuration for setup
└─────────────┘
```

## 🛠️ **Tool Registration Logic**

Based on `config.yaml` settings:

```yaml
tools:
  enable_file_tools: true   → File tools (Read, Write, Edit)
  enable_bash: true        → Bash tools (Bash, Kill, Output)  
  enable_zai_search: true  → Z.AI tools (web_search, web_reader)
  enable_skills: true      → Skills system (10+ skills)
  enable_mcp: true         → MCP tools (from mcp.json)
  enable_note: true        → Session note tool
```

## 🔗 **MCP Server Loading**

From `mcp.json`:
```json
{
  "mcpServers": {
    "memory": { "command": "npx -y @modelcontextprotocol/server-memory", ... },
    "filesystem": { "command": "npx -y @modelcontextprotocol/server-filesystem ...", ... },
    "git": { "command": "python -m mcp_server_git", ... }
  }
}
```

## ✅ **System Status Summary**

- **Configuration Files**: ✅ All in place (`mini_agent/config/`)
- **Environment**: ✅ `.env` loaded with API keys
- **Launch Script**: ✅ `launch_mini_agent.py` working
- **Tool Registration**: ✅ 39+ tools based on config
- **Dependencies**: ✅ All installed in `.venv`

**Result**: Clean, working Mini-Agent system ready for use!
